// Phase 0: static shell only. Staleness thresholds, default category,
// export/import/reset all land in Phase 7 — see phases.md.

export function App() {
  return (
    <div class="panel">
      <header class="panel-header">
        <span class="brand">Breadcrumb — Settings</span>
      </header>
      <main class="empty-state">
        <p>Settings arrive in Phase 7.</p>
      </main>
    </div>
  );
}
